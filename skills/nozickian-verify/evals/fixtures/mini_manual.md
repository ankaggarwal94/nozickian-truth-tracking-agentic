# Mini manual fixture

Product X CLI 2.4 supports `xctl backup --verify` on Linux and macOS. The command exits with code 0 only after checksum verification succeeds. Product X CLI 2.3 does not include `--verify`.
